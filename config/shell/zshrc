# Zsh configuration file
# This file is sourced by interactive zsh shells
# It integrates with the modular dotfiles configuration system
# Set DEBUG_SHELL_INIT=1 to enable debug output during shell initialization

# Powerlevel10k instant prompt (must stay near top; see p10k docs)
if [[ -r "${XDG_CACHE_HOME:-$HOME/.cache}/p10k-instant-prompt-${(%):-%n}.zsh" ]]; then
  source "${XDG_CACHE_HOME:-$HOME/.cache}/p10k-instant-prompt-${(%):-%n}.zsh"
fi

[[ -n "$DEBUG_SHELL_INIT" ]] && echo "[.zshrc] Loading .zshrc"

# Path to your Oh My Zsh installation.
export ZSH="$HOME/.oh-my-zsh"

# Powerlevel10k theme (config: config/shell/zsh/.p10k.zsh, symlinked to ~/.p10k.zsh)
ZSH_THEME="powerlevel10k/powerlevel10k"

# Plugins (git for branch in prompt; history-substring-search for key bindings below)
plugins=(git history-substring-search)

# Only load oh-my-zsh if present and not disabled (e.g., in Cursor Agent context)
if [[ -z "$DISABLE_OH_MY_ZSH" ]] && [[ -f "$ZSH/oh-my-zsh.sh" ]]; then
    source "$ZSH/oh-my-zsh.sh"
fi

# Apply Powerlevel10k config from dotfiles (same on every machine)
if [[ -f "${DOTFILES_ROOT:-$HOME/Development/dotfiles}/config/shell/zsh/.p10k.zsh" ]]; then
  source "${DOTFILES_ROOT:-$HOME/Development/dotfiles}/config/shell/zsh/.p10k.zsh"
fi

# User configuration

# Source modular configuration files
# env.sh, paths.sh, aliases.sh are loaded by .zshenv (all zsh invocations)
# Software.sh is interactive-only (install helpers, completions, etc.)
[[ -f ~/.config/shell/software.sh ]] && source ~/.config/shell/software.sh

# Zsh-specific configurations
# History configuration
HISTSIZE=10000
SAVEHIST=10000
HISTFILE=~/.zsh_history
setopt HIST_IGNORE_DUPS
setopt HIST_IGNORE_ALL_DUPS
setopt HIST_SAVE_NO_DUPS
setopt HIST_IGNORE_SPACE
setopt HIST_VERIFY
setopt SHARE_HISTORY

# Completion configuration
setopt AUTO_MENU
setopt COMPLETE_IN_WORD
setopt ALWAYS_TO_END

# kubectl completion for zsh (requires compdef from OMZ/compinit)
if command -v kubectl &> /dev/null && (( $+functions[compdef] )); then
    source <(kubectl completion zsh)
fi

# Directory navigation
setopt AUTO_PUSHD
setopt PUSHD_IGNORE_DUPS
setopt PUSHD_SILENT

# Other useful options
setopt EXTENDED_GLOB
setopt NO_CASE_GLOB
setopt NUMERIC_GLOB_SORT

# Key bindings (history-substring-search only when OMZ loaded the plugin)
(( $+functions[history-substring-search-up] )) && bindkey '^[[A' history-substring-search-up
(( $+functions[history-substring-search-down] )) && bindkey '^[[B' history-substring-search-down
bindkey '^[[1;5C' forward-word
bindkey '^[[1;5D' backward-word

# Custom functions and aliases can be added here
# Most are defined in ~/.config/shell/aliases.sh

# Example aliases (uncomment and modify as needed)
# alias zshconfig="mate ~/.zshrc"
# alias ohmyzsh="mate ~/.oh-my-zsh"

# Secrets and machine-specific env (tokens, PNPM_HOME, etc.) go in ~/.config/shell/private.sh

# Machine-specific config (not in repo): OrbStack, local overrides, etc.
[[ -f ~/.config/shell/local.sh ]] && source ~/.config/shell/local.sh
